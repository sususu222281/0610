rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Blanket access allowed for testing as specified by the user
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
